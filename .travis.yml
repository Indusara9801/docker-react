sudo: required
services:
  - docker

before_install:
  - docker build -t wadiukarunathilaka/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=TRUE wadiukarunathilaka/dockerreact npm run test -- --coverage